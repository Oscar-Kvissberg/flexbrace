{
  "name": "flexbrace",
  "version": "1.0.0",
  "private": true,
  "description": "Flexbrace Shopify theme",
  "scripts": {
    "dev": "shopify theme dev",
    "push": "shopify theme push",
    "check": "shopify theme check"
  },
  "devDependencies": {
    "@shopify/cli": "^4.7.1"
  }
}
